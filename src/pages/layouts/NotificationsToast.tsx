import { NotificationsToastProps } from "piral";
import React from "react";

export const NotificationsToast: React.ComponentType<
  NotificationsToastProps
> = ({ options, onClose, children }) => {
  return <></>;
};
